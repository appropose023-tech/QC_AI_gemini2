android {
    namespace "pcb_aoi_inspector" 
    compileSdk flutter.compileSdkVersion

    defaultConfig {
        applicationId "pcb_aoi_inspector" // This is the 'Package Name'
        minSdkVersion 21
        targetSdkVersion flutter.targetSdkVersion
        versionCode 1
        versionName "1.0"
    }
}
